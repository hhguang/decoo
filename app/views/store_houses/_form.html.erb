<%= simple_form_for(@store_house) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name,:label=>"名称" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit,:value=>"保存" %>
    或者<%= link_to 'Back', store_houses_path %>
  </div>

<% end %>
